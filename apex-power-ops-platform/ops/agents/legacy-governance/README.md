# Legacy Governance Patterns

This directory preserves reusable workspace-governance and operator-pattern material imported from the legacy APEX root during packet `2026-04-13-apex-unification-001a`.

Status:
- Non-authoritative pattern library
- Useful for lineage, migration context, and pattern extraction
- Not automatically binding on the current unified platform

Current authority:
- Active packet, delegation, and operator rules live under `ops/agents/` and the approved authority documents
- Any legacy pattern must be explicitly promoted before it becomes active guidance

Imported slices:
- `root/`: legacy workspace design and protocol documents
- `sessions/`: selected reusable session and operator guidance retained for pattern reference

Archive boundary:
- One-off session residue, logs, templates, and superseded handoffs were routed to `archive/legacy-repos/apex-platform/sessions/`
- This directory intentionally excludes raw session churn
