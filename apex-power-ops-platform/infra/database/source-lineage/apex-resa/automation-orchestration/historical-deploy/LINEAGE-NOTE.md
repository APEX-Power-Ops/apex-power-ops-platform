# Historical Deploy Wrapper Note

`DEPLOY_ORCHESTRATION.sql` is retained here as historical lineage context only.

It documents how the legacy `Supabase` root wrapped the already-landed orchestration lineage files.

Do not treat this location as a live deployment entrypoint.

Active authority remains the decomposed lineage SQL under the parent automation-orchestration slice and the governed platform operator surface.