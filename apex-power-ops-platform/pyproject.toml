[project]
name = "apex-power-ops-platform"
version = "0.1.0"
description = "Bootstrap Python workspace metadata for the Apex Power Ops platform monorepo"
requires-python = ">=3.11"

[tool.pytest.ini_options]
addopts = "-q"