# Transient probe used during packet-019g authoring.  Replaced with the
# results inline in the handoff doc; retained as an empty file so the
# author trail is reproducible without leaving executable dead code.
