# Root Working Notes

This folder preserves early root-level planning notes as architecture lineage only.

Do not treat these notes as current platform authority.
Use `Platform-Authority/` and the current staged platform handoffs for active decisions.
