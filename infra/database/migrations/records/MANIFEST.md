# NETA Records Domain SQL Migration Manifest
## Packet: 2026-06-12-records-001
## Lane: NETA field-records platform — Chip 1 (data model foundation)
## Authority: `reference/records/00-MASTER-INDEX.md` §4, `01-OFFLINE-SYNC-ARCHITECTURE.md`

The `records` schema is the in-house replacement for the legacy field-test datastore:
asset register, NETA data sheets, test results, and PM tracking. This Chip 1 set
lands the foundation tables only — no data seed, no cross-schema FK activation
(both are later chips).

**Generalized 2026-06-15 (records chip):** the original `neta.*` sketch is renamed to `records.*` and the `datasheet_*` tables to `form_*` with a `form_type` discriminator (`neta_datasheet` | `jha` | `safety` | `sop` | `inspection`), plus a deferred `assets.equipment_model_id` soft seam to the `core` identity spine. NETA data sheets are one `form_type`; the engine is generic. Applied + down/up-validated on a local Postgres `records_dev`.

The model was validated against the legacy field-test baseline — the capability
floor recorded in `reference/records/02-LEGACY-BASELINE.md`. Refinements
folded in: sheet-level As-Found/As-Left, GPS + site location hierarchy on assets,
the `job_number` external join key, a `graph` result kind, and the field_schema
control model.

## Execution Order

Execute in strict sequential order against the target database:

| Order | File | Contents | Dependencies |
| --- | --- | --- | --- |
| 1 | `001_records_enums.sql` | `records` schema + 12 enum types | None (`gen_random_uuid()` support) |
| 2 | `002_records_tables.sql` | 8 foundation tables with PKs, internal FKs, CHECK + UNIQUE constraints, sync-contract columns, and the reciprocal form_submissions↔pm_events FK | Requires 001 |
| 3 | `003_records_indexes.sql` | FK-join + filter indexes; partial unique index for one current template version per code | Requires 002 |
| 4 | `004_records_triggers_and_views.sql` | `updated_at` auto-maintenance on all 8 tables + 2 read views (`v_asset_test_history`, `v_pm_due`) | Requires 002 |
| 5 | `005_neta_reference_tables.sql` | **Chip 2a** — NETA reference layer: `neta_procedures` + `neta_test_items` + `neta_tables` + the `neta_test_category_enum` | Requires 001 |
| 6 | `006_neta_reference_seed.sql` | **Chip 2a** — accurate seed from the NETA master equipment table (ANSI/NETA ATS-2025 / MTS-2023): **72 procedures · 3,920 test items · 43 acceptance tables**. Items split visual/electrical/test-value + a `crossref` category carrying the "Refer to Section X" composition pointers of crossref-only procedures (the MCCs). GENERATED by `gen_neta_seed.py`; idempotent (`ON CONFLICT`), deterministic UUID5 ids | Requires 005 |
| 7 | `007_asset_class_shell.sql` | **Chip 2-shell** — the `asset_classes` family taxonomy (2-level): **27 NETA-category parents** (19 active + 8 future) + **40 practical leaf classes** (every active NETA procedure has a leaf home, incl. the motor-control 2×2, the LTC, and DC machines). Parents carry `neta_category` (the anchor into `neta_procedures`); leaves carry templates/assets later. GENERATED by `gen_shell_seed.py`; idempotent, deterministic UUID5 | Requires 002 (+ 005 for the anchor) |
| 8 | `008_backfill_tables.sql` | **Chip 2-backfill** — `asset_class_neta_procedure` (leaf→procedure links) + `neta_procedure_xref` (procedure→procedure graph) + `neta_xref_kind_enum` | Requires 005 + 007 |
| 9 | `009_backfill_seed.sql` | **Chip 2-backfill** — seed: **61 leaf→procedure links** (RESERVED excluded; one primary/leaf) + **70 xref edges** (8 `crossref` composition + 62 `in_accordance` method-borrowing). GENERATED by `gen_backfill_seed.py`; idempotent, deterministic UUID5 | Requires 008 |
| 10 | `010_lv_cb_datasheet_template.sql` | **Chip 2b** — the first datasheet: the LV circuit-breaker composite `form_templates` row (`ats_lv_cb_v1`, bound to the `cb_lv` leaf). A `construction_type` selector (power/insulated_case/molded_case) gates the molded-case deltas via `visible_if` — one template, not three; **11 sections**; acceptance windows DECLARED via `tolerance_source`→tcc (resolved at provisioning, Chip 5). `field_schema` covers all **25 NETA 7.6.1.2 ATS items** (coverage invariant). GENERATED by `gen_lv_cb_template.py`; idempotent, deterministic UUID5 | Requires 002 + 005 + 007 |
| 11 | `011_mvhv_cb_datasheet_template.sql` | **Chip 2b** — the MV/HV circuit-breaker composite `form_templates` row (`ats_mvhv_cb_v1`, bound to the `cb_mvhv` leaf). An `interrupting_medium` selector (air/oil/vacuum/sf6) gates each medium's deltas via `visible_if` — one template, not four; **15 sections**; acceptance windows DECLARED via `tolerance_source` → **neta_table + mfr** (NOT tcc: an MV/HV breaker has no integral trip curve — protection is external relays, 7.9 / relaytcc; resolved at provisioning, Chip 5). `field_schema` covers all **122 NETA ATS items across the 4 interrupting-medium procedures 7.6.1.3/7.6.2/7.6.3/7.6.4** (union coverage invariant). GENERATED by `gen_mv_cb_template.py`; idempotent, deterministic UUID5 | Requires 002 + 005 + 007 |
| 12 | `012_xfmr_datasheet_templates.sql` | **Chip 2b** — TWO transformer composites in one migration: `ats_dry_xfmr_v1` (bound to `xfmr_dry`; folds small 7.2.1.1 + large 7.2.1.2 via a `dry_class` selector; **13 sections**; 35-item union coverage) and `ats_liquid_xfmr_v1` (bound to `xfmr_liquid`; 7.2.2; **17 sections**; 39 items, incl. LTC 7.12.3 / IT 7.10 / surge-arrester 7.19 cross-ref borrows). BOTH fold two-winding/three-winding via a `winding_config` selector that gates the tertiary-winding (H-Y/X-Y/Y-G) measurement rows. Acceptance windows DECLARED via `tolerance_source` → **neta_table** (IR→Table **100.5**, torque→100.12) + **mfr** (winding resistance, bushing PF, turns-ratio); oil/DGA = standard-basis (ASTM D923 / IEEE C57.104); NOT tcc. GENERATED by `gen_xfmr_template.py`; idempotent, deterministic UUID5 | Requires 002 + 005 + 007 |
| 13 | `013_switchgear_templates.sql` | **Chip 2b** — TWO assembly datasheets: `ats_switchgear_v1` (NETA 7.1.1; **13 sections**; 35 ATS items; an `assembly_type` selector (LV switchboard / MV metal-clad / pad-mount / VFI) for context — **bound to the PARENT `switchgear` node** because four leaves share the 7.1.1 procedure, so it cannot 1:1-bind to a leaf the way the prior sheets did) and `ats_panelboard_v1` (NETA 7.1.2; **6 sections**; 14 ATS items; leaf-bound to `swgr_panelboard`). Acceptance windows DECLARED via `tolerance_source` → **neta_table** (IR→100.1, dielectric→100.2, torque→100.12) + **mfr** (bolted-connection resistance); IT 7.10 / SA 7.19 / ground 7.13 / metering 7.11 = cross-ref borrows; NOT tcc. GENERATED by `gen_switchgear_template.py`; idempotent, deterministic UUID5 | Requires 002 + 005 + 007 |
| 14 | `014_cable_templates.sql` | **Chip 2b** — TWO cable datasheets (leaf-bound, one procedure each): `ats_lv_cable_v1` (NETA 7.3.2, LV 600 V max; **6 sections**; 11 ATS) and `ats_mvhv_cable_v1` (NETA 7.3.3, MV/HV; **11 sections**; 18 ATS — adds the shielded-cable HV battery: shield continuity, TDR, jacket integrity, dielectric withstand, tan-delta, PD). Acceptance windows DECLARED via `tolerance_source` → **neta_table** (MV IR→100.1, tan-delta→100.6.6, torque→100.12) + **mfr** (LV IR comparison, MV dielectric withstand); NOT tcc. GENERATED by `gen_cable_template.py`; idempotent, deterministic UUID5 | Requires 002 + 005 + 007 |
| 15 | `015_it_datasheet_templates.sql` | **Chip 2b** — THREE instrument-transformer datasheets (leaf-bound, one procedure each): `ats_ct_v1` (NETA 7.10.1; **12 sections**; 18 ATS), `ats_vt_v1` (NETA 7.10.2; **11 sections**; 18 ATS), `ats_cvt_v1` (NETA 7.10.3; **12 sections**; 20 ATS — adds the CVT capacitor-section battery: bolted-resistance + capacitance + PF/DF). Acceptance windows DECLARED via `tolerance_source` → **neta_table** (IR→100.5, dielectric→100.9, torque→100.12) + **mfr** (PF/DF); NOT tcc. **FIRST capture-mode-aware build:** a template `capture` block + an `attachments` cover section (Path 1 = datasheet-as-cover for the mfr/OEM report) + `instrument_import` hints wiring each instrument-fillable section to its bridge (CT excitation/ratio→`ct_analyzer`, VT/CVT ratio→`ptm` TX_TTR, PF/DF→`dtax` TX_PFDF; Path 2 = the Chip-10 importer). **FIRST PowerDB-anchored family** (27660 CT / 27600 PT-VT / 99000 CVT). GENERATED by `gen_it_template.py`; idempotent, deterministic UUID5 | Requires 002 + 005 + 007 |
| 16 | `016_grounding_template.sql` | **Chip 2b** — the Grounding-System datasheet `ats_grounding_v1` (leaf-bound to `grounding_system`; NETA 7.13; **8 sections**; 6 ATS = 3 VM + 3 electrical: bolted-connection resistance, ground resistance (fall-of-potential / IEEE 81), point-to-point). **NOT PowerDB-anchored** (no grounding form in the corpus) → NETA-derived. Acceptance windows DECLARED via `tolerance_source` → **mfr** only (ground resistance vs design / IEEE 81; bolted-connection R) — NO `tcc`, and NO `neta_table` window (7.13 has no IR/dielectric table; torque → 100.12 is an acceptance basis on the VM row). Capture = `field` + `cover_attach` only (no instrument bridge for ground testers). GENERATED by `gen_grounding_template.py`; idempotent, deterministic UUID5 | Requires 002 + 005 + 007 |
| 17 | `017_surge_templates.sql` | **Chip 2b** — Surge-Protection: a **taxonomy split** (operator ruling) + TWO leaf-bound sheets. The split adds the **`spd_lv`** leaf under `surge_protection` and relinks 7.19.1 → spd_lv (dropping the stale `surge_arrester`→7.19.1 link); `surge_arrester` keeps 7.19.2. Then `ats_lv_spd_v1` (`spd_lv`; NETA 7.19.1; **7 sections**; 8 ATS) + `ats_mvhv_arrester_v1` (`surge_arrester`; NETA 7.19.2; **9 sections**; 12 ATS — bolted-R + IR (Table 100.1) + grounding + watts-loss). **NOT PowerDB-anchored** → NETA-derived. Acceptance windows DECLARED via `tolerance_source` → **neta_table** (MV IR→100.1) + **mfr** (watts-loss; grounding) — NO `tcc`; LV SPD has no NETA table so its only window is the mfr grounding slot. Capture = `field` + `cover_attach` (no surge instrument bridge). Idempotent UUID5; the split is reversible in `_down`. GENERATED by `gen_surge_template.py` | Requires 002 + 005 + 007 (+ 009 for the acnp relink) |
| 18 | `018_motor_templates.sql` | **Chip 2b** — THREE rotating-machinery datasheets (leaf-bound; the largest family, 84 ATS): `ats_induction_motor_v1` (`rm_induction`; NETA 7.15.1; **14 sections**; 27 ATS), `ats_synchronous_machine_v1` (`rm_synchronous`; NETA 7.15.2; **15 sections**; 37 ATS — adds the field/excitation battery), `ats_dc_machine_v1` (`rm_dc`; NETA 7.15.3; **11 sections**; 20 ATS). **NOT PowerDB-anchored** → NETA-derived. Acceptance windows DECLARED via `tolerance_source` → **mfr** only (IR/PI per IEEE 43, hipot per NEMA MG 1, winding R + PF/DF per mfr) — NO `tcc`, NO `neta_table` window (motors test to IEEE 43 / NEMA, not a NETA table; torque → 100.12 = VM acceptance basis). Capture = `field` + `cover_attach` (no motor instrument bridge). GENERATED by `gen_motor_template.py`; idempotent, deterministic UUID5 | Requires 002 + 005 + 007 |
| 19 | `019_motor_starter_templates.sql` | **Chip 2b** — TWO motor-starter datasheets (leaf-bound, one complete 7.16.1 procedure each): `ats_lv_motor_starter_v1` (`motor_starter_lv`; NETA 7.16.1.1; **8 sections**; 14 ATS) and `ats_mv_motor_starter_v1` (`motor_starter_mv`; NETA 7.16.1.2; **12 sections**; 29 ATS — adds the MV battery: vacuum-interrupter integrity (MAC + bottle), contact/coil/fuse resistance, and an `associated_devices` cross-reference group for the many "test X per Section Y" borrows (CPT 7.1 / starting xfmr 7.2.1 / reactor 7.20.3 / motor protection 7.9 / IT 7.10 / metering 7.11)). **PowerDB-anchored** (31000 Motor Starter / 31300 MV Vacuum Motor Starter). Acceptance windows DECLARED via `tolerance_source` → **neta_table** (IR→100.1) + **mfr** (motor-protection / dielectric per mfr data); NOT tcc; torque→100.12 = VM acceptance basis. Capture = `field` + `cover_attach` (no starter instrument bridge). **MCC scope ruling:** the two MCC leaves (`mcc_lv`→7.16.2.1, `mcc_mv`→7.16.2.2) are pure `crossref` (no own NETA items) → deferred to asset-tree composition, NOT standalone sheets. GENERATED by `gen_motor_starter_template.py`; idempotent, deterministic UUID5 | Requires 002 + 005 + 007 |
| 20 | `020_xfmr_capture_mode.sql` | **Chip 10a (Phase 1)** — make the transformer templates capture-mode-aware (the PTM import targets): adds the `capture` block + marks `turns_ratio` / `winding_resistance` / `power_factor` / `excitation_current` `instrument_import` with `import {tool: ptm, profile}` on `ats_liquid_xfmr_v1` + `ats_dry_xfmr_v1`. Section set unchanged. GENERATED by `gen_020_xfmr_capture_mode.py` (reads + patches the live `field_schema`); idempotent; reversible `_down`; `test_020` 4/4 on `records_dev`. | Requires 012 |
| 21 | `021_xfmr_pf_readings.sql` | **Records reconcile slice 1 / D-R1** — add the Doble raw-reading columns (`test_kv` / `current_ma` / `watts`, around `pf_pct`) + `pf_corrected_pct` (calc) to the `power_factor` table on `ats_liquid_xfmr_v1` + `ats_dry_xfmr_v1`. Surfaced by reconciliation vs the Prime 56900 form (`.audit_workspace/records_reconcile/SLICE1-transformer.md`): our PF section was too shallow for a field Doble PF test (and for DTAX/PTM import). Additive (no NETA-coverage change). GENERATED by `gen_021_xfmr_pf_readings.py` (read-live snapshot; preserves the 020 capture-mode); idempotent; reversible `_down`; `test_021` 5/5 on `records_dev`. | Requires 012 (+020) |

Each file has a matching `_down.sql` that reverses it in dependency order. Chip 2a
is validated by `test_005_neta_reference.py` (9 tests); the Chip 2-shell family
taxonomy by `test_007_asset_class_shell.py` (13 tests); Chip 2-backfill by
`test_008_backfill.py` (8 tests); the Chip 2b LV-CB datasheet by
`test_010_lv_cb_template.py` (17 tests — incl. the NETA 7.6.1.2 coverage invariant); the
Chip 2b MV/HV-CB datasheet by `test_011_mvhv_cb_template.py` (21 tests — incl. the 122-item
union coverage invariant across the 4 interrupting-medium procedures + the R-A neta_table/mfr
tolerance-source bindings); the Chip 2b transformer datasheets by `test_012_xfmr_template.py`
(17 tests — incl. the per-sheet dry-35 / liquid-39 coverage invariants + the winding_config
(2W/3W) and dry_class (small/large) folds); and the Chip 2b switchgear + panelboard datasheets
by `test_013_switchgear_template.py` (15 tests — incl. the parent-binding of the shared-7.1.1
switchgear sheet + the per-sheet 35 / 14 coverage invariants); and the Chip 2b cable datasheets
by `test_014_cable_template.py` (13 tests — incl. the per-sheet LV-11 / MV-18 coverage invariants
+ the MV/HV shielded-cable battery); and the Chip 2b instrument-transformer datasheets by
`test_015_it_template.py` (17 tests — incl. the per-sheet CT-18 / VT-18 / CVT-20 coverage invariants
+ the NEW capture-mode contract: template `capture` block, the `attachments` cover section, and the
`instrument_import` bridge hints); and the Chip 2b grounding datasheet by
`test_016_grounding_template.py` (13 tests — incl. the 7.13 6-item coverage invariant + the
mfr-only R-A + the field/cover_attach capture, no instrument bridge); and the Chip 2b surge
datasheets by `test_017_surge_template.py` (16 tests — incl. the leaf-split assertions (spd_lv added,
7.19.1 relinked, reversal restores `surge_arrester`→7.19.1) + the per-sheet LV-8 / MV-12 coverage
invariants + the MV IR→100.1 binding); and the Chip 2b rotating-machinery datasheets by
`test_018_motor_template.py` (14 tests — incl. the per-sheet induction-27 / synchronous-37 / DC-20
coverage invariants + the mfr/IEEE-43 IR slot + the synchronous/DC field-excitation battery); and the
Chip 2b motor-starter datasheets by `test_019_motor_starter_template.py` (14 tests — incl. the per-sheet
LV-14 / MV-29 coverage invariants + the IR→neta_table 100.1 binding + the MV-only vacuum /
contact-resistance / associated-devices battery) — all on local `records_dev`.

> **Run the records tests per-chip, not as one `pytest .` pass.** Each test file's
> fixture does a destructive down→up of its own migration. Once cross-chip FKs exist
> (`008`→`neta_procedures`, `010`-`014`→`asset_classes`), an in-file-order combined run tears
> tables down out of dependency order and corrupts the dev DB. If that happens, reset in
> reverse order — `019_down → 018_down → 017_down → 016_down → 015_down → 014_down → 013_down → 012_down → 011_down → 010_down → 008_down → 005_down` — then re-apply
> `005→006→008→009→010→011→012→013→014→015→016→017→018→019`.
>
> The xfmr patches (`020` capture-mode, `021` PF-readings) layer on `012` and are snapshot-up /
> surgical-down — after any base reset + re-apply, re-run `020` then `021` to restore them. Because the
> ups are full snapshots, re-running an *older* xfmr snapshot (e.g. `020`) reverts a *newer* one (`021`);
> always re-apply forward to the latest xfmr patch.

## Quick Execution

```bash
# Against a local staging database
psql -d apex_neta_stage -f 001_records_enums.sql
psql -d apex_neta_stage -f 002_records_tables.sql
psql -d apex_neta_stage -f 003_records_indexes.sql
psql -d apex_neta_stage -f 004_records_triggers_and_views.sql

# Roll back (reverse order)
psql -d apex_neta_stage -f 004_records_triggers_and_views_down.sql
psql -d apex_neta_stage -f 003_records_indexes_down.sql
psql -d apex_neta_stage -f 002_records_tables_down.sql
psql -d apex_neta_stage -f 001_records_enums_down.sql
```

> Not yet applied to the governed Supabase project — this lands as reviewable SQL
> first (per the operator's "talk about how it works before migration steps" gate).

## Object Summary

| Category | Count | Details |
| --- | --- | --- |
| Schema | 1 | `records` |
| Enum types | 12 | provenance_source, provenance_status, asset_status, asset_condition, neta_standard, form_status, form_type, as_found_as_left, assessment_result, field_value_kind, pm_interval_unit, pm_event_status |
| Tables | 8 | asset_classes, assets, form_templates, form_submissions, form_field_values, pm_programs, pm_schedules, pm_events |
| Indexes | 25 | FK-join + filter; incl. partial `uq_form_templates_current` and partial `ix_pm_schedules_due` |
| Trigger functions | 1 | `records.fn_set_updated_at` |
| Triggers | 8 | `updated_at` on every table |
| Views | 2 | `v_asset_test_history`, `v_pm_due` |

## Deferred (NOT in Chip 1)

- **Cross-schema FK activation** — `site_ref`, `client_ref`, `project_ref`,
  `work_package_ref`, `apparatus_ref` are soft UUID columns (no hard FK) to avoid
  seed-ordering coupling. Activation = punch-list Chip 8.
- **Data seed** — the NETA reference layer (procedures / test-items / acceptance
  tables) is seeded in **Chip 2a** (005/006); the **asset_class family taxonomy** (the
  2-level NETA-anchored shell) in **Chip 2-shell** (007); the **leaf↔procedure links +
  procedure xref graph** in **Chip 2-backfill** (008/009). The **datasheet
  `field_schema`s** land as Chip 2b repeats: LV circuit breaker (`ats_lv_cb_v1`, 010,
  `04-LV-CB-DATASHEET-SPEC.md`), MV/HV circuit breaker (`ats_mvhv_cb_v1`, 011,
  `05-MVHV-CB-DATASHEET-SPEC.md`), transformers (`ats_dry_xfmr_v1` + `ats_liquid_xfmr_v1`,
  012, `06-TRANSFORMER-DATASHEET-SPEC.md`), and switchgear (`ats_switchgear_v1` + `ats_panelboard_v1`,
  013, `07-SWITCHGEAR-DATASHEET-SPEC.md`), and cables (`ats_lv_cable_v1` + `ats_mvhv_cable_v1`,
  014, `08-CABLE-DATASHEET-SPEC.md`), and instrument transformers (`ats_ct_v1` + `ats_vt_v1` +
  `ats_cvt_v1`, 015, `09-IT-DATASHEET-SPEC.md` — the first **capture-mode-aware** + **PowerDB-anchored**
  family), and grounding (`ats_grounding_v1`, 016, `10-GROUNDING-DATASHEET-SPEC.md` — NETA-derived,
  mfr-only R-A, field/cover_attach capture), and surge protection (`ats_lv_spd_v1` + `ats_mvhv_arrester_v1`,
  017, `11-SURGE-DATASHEET-SPEC.md` — a taxonomy split adds the `spd_lv` leaf; NETA-derived), and
  rotating machinery (`ats_induction_motor_v1` + `ats_synchronous_machine_v1` + `ats_dc_machine_v1`,
  018, `12-MOTOR-DATASHEET-SPEC.md` — the largest family, NETA-derived), and motor starters
  (`ats_lv_motor_starter_v1` + `ats_mv_motor_starter_v1`, 019, `13-MOTOR-STARTER-DATASHEET-SPEC.md` —
  PowerDB-anchored; the two MCC leaves deferred as `crossref` asset-tree composition) — each referencing
  + filtering the leaf's linked NETA items with a coverage invariant. The operator-priority queue
  (breakers → transformers → switchgear → cables → IT → grounding → SA → motors) is COMPLETE; **motor
  control (7.16) is the first post-queue family**, on the same recipe; further NETA categories follow.
- **Sync rules + uploadData connector** (PowerSync) = Chip 3, not a SQL migration.
