# Secrets now SOURCED from self-hosted Infisical (NAMES only -- NEVER values).
# Arming a name here makes infra/secret-audit.sh Check 1c FAIL if a stale copy
# still lingers in a local cache (infra/.env or the offsite-backup env).
# Empty (header only) = nothing fully migrated yet (consumers still read the caches).
#
# Add one secret NAME per line as each consumer cuts over to inject.sh.
